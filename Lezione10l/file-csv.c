#include <stdio.h>
#include <string.h>

int * countRC(FILE * fp)
int main (int argc, char * argv[])
{
    FILE *fp;
    char c;
    int i,j=1,x,col=1;
    int v[3];
    int min=99,max=0;
    double avg;
    if((fp=fopen("file.csv"/*argv[1]*/,"r"))==NULL)
        return -1;
    
        
    
    fclose(fp);


}